name: heroku-release
version: 0.0.2
summary: Releases already pushed image to heroku
tags:
  - deploy
  - heroku
  - docker
  - release
properties:
  - name: app-name
    type: string
    required: true

  - name: username
    type: string
    required: true

  - name: password
    type: string
    required: true
